{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n Server,Client -c green,blue \"npm run server\" \"npm run client\"",
    "client": "cd client && npm start",
    "server": "cd server && npm start"
  }
}
